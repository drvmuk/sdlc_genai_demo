[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "customer_order_processing"
version = "1.0.0"
description = "Customer and Order data processing pipeline using Delta Live Tables"
authors = [
    {name = "Databricks Engineer", email = "engineer@example.com"}
]
readme = "README.md"
requires-python = ">=3.8"

[project.dependencies]
pyspark = ">=3.3.0"
delta-spark = ">=2.2.0"