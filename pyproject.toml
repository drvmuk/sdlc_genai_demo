[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "customer-order-data-processor"
version = "1.0.0"
description = "Databricks PySpark application for processing customer and order data with SCD Type 2 implementation"
authors = [
    {name = "Data Engineering Team", email = "data-engineering@example.com"}
]
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
"Homepage" = "https://github.com/example/customer-order-data-processor"
"Bug Tracker" = "https://github.com/example/customer-order-data-processor/issues"