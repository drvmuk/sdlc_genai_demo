**Functional Requirements Document (FRD) for Data Platform**
===========================================================

**Introduction**
---------------

The purpose of this document is to outline the functional requirements for the Data Platform project, which aims to integrate customer and order data from CSV files into Delta tables, perform data cleansing and transformation, and create aggregated reports.

**Functional Requirements**
-------------------------

1. **Data Ingestion**
	* The system shall read CSV data from the specified volumes: `/Volumes/gen_ai_poc_databrickscoe/sdlc_wizard/customerdata` and `/Volumes/gen_ai_poc_databrickscoe/sdlc_wizard/orderdata`.
	* The system shall load the data into Delta tables `customer` and `order` with the specified schema.

2. **Data Cleansing**
	* The system shall remove "Null"/Null records from both `customer` and `order` tables.
	* The system shall remove duplicate records from both `customer` and `order` tables.

3. **Data Transformation and Loading**
	* The system shall create an `ordersummary` table if it does not exist in the catalog "gen_ai_poc_databrickscoe" and schema "sdlc_wizard" with the specified schema.
	* The system shall join the `customer` and `order` data using the "CustId" field and load the data into an SCD Type 2 table `ordersummary`.
	* The system shall update the SCD Type 2 table `ordersummary` whenever there is a change in the `customer` table.

4. **Aggregated Reporting**
	* The system shall create a `customeraggregatespend` table if it does not exist in the catalog "gen_ai_poc_databrickscoe" and schema "sdlc_wizard" with the specified schema.
	* The system shall aggregate the "TotalAmount" column from the `ordersummary` table and group it by the "Name" and "Date" columns.
	* The system shall load the aggregated data into the `customeraggregatespend` table.

**System Flow Diagram**
```mermaid
graph TD;
ReadCSV[Read CSV Data] --> LoadDelta[Load Data into Delta Tables];
LoadDelta --> CleanseData[Remove Null/Null and Duplicate Records];
CleanseData --> TransformData[Join Customer and Order Data];
TransformData --> LoadSCDType2[Load Data into SCD Type 2 Table];
LoadSCDType2 --> AggregateData[Aggregate Data for Reporting];
AggregateData --> LoadAggregatedData[Load Aggregated Data into customeraggregatespend Table];
```

**Process Flow / Flowchart**
```mermaid
graph TD;
Start[Start] --> ReadCustomerCSV[Read Customer CSV Data];
ReadCustomerCSV --> LoadCustomerDelta[Load Customer Data into Delta Table];
ReadOrderCSV[Read Order CSV Data] --> LoadOrderDelta[Load Order Data into Delta Table];
LoadCustomerDelta --> CleanseCustomerData[Remove Null/Null and Duplicate Records from Customer Data];
LoadOrderDelta --> CleanseOrderData[Remove Null/Null and Duplicate Records from Order Data];
CleanseCustomerData --> TransformData[Join Customer and Order Data];
CleanseOrderData --> TransformData;
TransformData --> LoadSCDType2[Load Data into SCD Type 2 Table ordersummary];
LoadSCDType2 --> AggregateData[Aggregate Data for Reporting];
AggregateData --> LoadAggregatedData[Load Aggregated Data into customeraggregatespend Table];
LoadAggregatedData --> End[End];
```

**Data Flow Diagram (DFD)**
```mermaid
graph TD;
CustomerCSV[Customer CSV Data] --> ReadCustomerCSV[Read Customer CSV Data];
OrderCSV[Order CSV Data] --> ReadOrderCSV[Read Order CSV Data];
ReadCustomerCSV --> LoadCustomerDelta[Load Customer Data into Delta Table];
ReadOrderCSV --> LoadOrderDelta[Load Order Data into Delta Table];
LoadCustomerDelta --> CleanseCustomerData[Remove Null/Null and Duplicate Records from Customer Data];
LoadOrderDelta --> CleanseOrderData[Remove Null/Null and Duplicate Records from Order Data];
CleanseCustomerData --> TransformData[Join Customer and Order Data];
CleanseOrderData --> TransformData;
TransformData --> LoadSCDType2[Load Data into SCD Type 2 Table ordersummary];
LoadSCDType2 --> AggregateData[Aggregate Data for Reporting];
AggregateData --> LoadAggregatedData[Load Aggregated Data into customeraggregatespend Table];
```

**High-Level Architecture Diagram**
```mermaid
graph TD;
CSVData[CSV Data] --> DataIngestion[Data Ingestion Layer];
DataIngestion --> DeltaTables[Delta Tables];
DeltaTables --> DataCleansing[Data Cleansing Layer];
DataCleansing --> DataTransformation[Data Transformation Layer];
DataTransformation --> SCDType2Table[SCD Type 2 Table ordersummary];
SCDType2Table --> AggregatedReporting[Aggregated Reporting Layer];
AggregatedReporting --> customeraggregatespendTable[customeraggregatespend Table];
```