**Concise Functional Requirements Document (FRD) for the "Application"**
====================================================================

### Overview

The "Application" is a data processing pipeline designed to load customer and order data from CSV files into Delta tables, perform data cleansing, and create aggregated summaries.

### Functional Requirements

1. **Data Ingestion**
	* Read CSV data from specified locations and load it into Delta tables "customer" and "order".
	* CSV file paths: `/Volumes/gen_ai_poc_databrickscoe/sdlc_wizard/customerdata` and `/Volumes/gen_ai_poc_databrickscoe/sdlc_wizard/orderdata`.
2. **Data Cleansing**
	* Remove "Null"/null and duplicate records from "customer" and "order" tables.
3. **Data Transformation and Loading**
	* Create "ordersummary" table if it does not exist in catalog "gen_ai_poc_databrickscoe" and schema "sdlc_wizard".
	* Join "customer" and "order" data using "CustId" field and load into SCD type 2 table "ordersummary".
4. **SCD Type 2 Implementation**
	* Update SCD type 2 "ordersummary" table when there are changes in "customer" table.
	* Make old records inactive and new records active.
	* Update `StartDate` and `EndDate` accordingly.
5. **Aggregated Summary**
	* Create "customeraggregatespend" table if it does not exist in catalog "gen_ai_poc_databrickscoe" and schema "sdlc_wizard".
	* Aggregate "TotalAmount" from "ordersummary" table and group by "Name" and "Date" columns.
	* Load aggregated data into "customeraggregatespend" table.

### Diagrams

#### System Flow Diagram
```mermaid
graph TD;
A[Start] --> B[Read CSV Data];
B --> C[Load Data into Delta Tables];
C --> D[Perform Data Cleansing];
D --> E[Create ordersummary Table];
E --> F[Load Joined Data into ordersummary];
F --> G[Implement SCD Type 2 Logic];
G --> H[Create customeraggregatespend Table];
H --> I[Load Aggregated Data into customeraggregatespend];
I --> J[End];
```

#### Process Flow / Flowchart
```mermaid
graph TD;
A[Start] --> B{CSV Data Available?};
B -->|Yes| C[Read CSV Data];
B -->|No| J[End];
C --> D[Load Data into Delta Tables];
D --> E{Data Cleansing Required?};
E -->|Yes| F[Perform Data Cleansing];
E -->|No| G[Create ordersummary Table];
F --> G;
G --> H[Load Joined Data into ordersummary];
H --> I{SCD Type 2 Logic Required?};
I -->|Yes| K[Implement SCD Type 2 Logic];
I -->|No| L[Create customeraggregatespend Table];
K --> L;
L --> M[Load Aggregated Data into customeraggregatespend];
M --> J;
```

#### Data Flow Diagram (DFD)
```mermaid
graph TD;
A[CSV Data] --> B[Data Ingestion Process];
B --> C[customer Delta Table];
B --> D[order Delta Table];
C --> E[Data Cleansing Process];
D --> E;
E --> F[ordersummary Table];
F --> G[SCD Type 2 Logic Process];
G --> H[ordersummary Table];
H --> I[Aggregated Summary Process];
I --> J[customeraggregatespend Table];
```

#### Sequence Diagram
```mermaid
graph TD;
A[Application] --> B[Read CSV Data];
B --> C[Load Data into Delta Tables];
C --> D[Perform Data Cleansing];
D --> E[Create ordersummary Table];
E --> F[Load Joined Data into ordersummary];
F --> G[Implement SCD Type 2 Logic];
G --> H[Create customeraggregatespend Table];
H --> I[Load Aggregated Data into customeraggregatespend];
```

#### High-Level Architecture Diagram
```mermaid
graph TD;
A[CSV Data] --> B[Data Ingestion Layer];
B --> C[Delta Tables];
C --> D[Data Processing Layer];
D --> E[ordersummary Table];
E --> F[SCD Type 2 Logic Layer];
F --> G[ordersummary Table];
G --> H[Aggregated Summary Layer];
H --> I[customeraggregatespend Table];
```