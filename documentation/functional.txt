Here are the detailed Functional Requirements based on the provided Business Requirement Document (BRD) and Coding Requirements:

### FR-SQL-001: Generate SQL Query for Intermediate Table "s_shared.sales_orders_hdr_itm_lots"

* **Title:** Generate SQL Query for Intermediate Table "s_shared.sales_orders_hdr_itm_lots"
* **Description:** The system must generate a complete SQL SELECT statement that applies transformation logic, includes appropriate joins, and ensures correct data types and formatting for the intermediate table "s_shared.sales_orders_hdr_itm_lots".
* **Preconditions:**
	+ The input mapping table is available with the required columns.
	+ The source tables (e.g., "b_lots.plooh", "b_lots.plool") are available and contain the required data.
* **Main Flow / Functional Steps:**
	1. Retrieve the input mapping table with the required columns (Column Name, Column Type, Source Table, Source Column/Transformation Logic, Join Condition).
	2. Parse the input mapping table to identify the required columns, data types, and transformation logic for the intermediate table.
	3. Generate the SQL SELECT statement with the required columns, applying transformation logic as specified.
	4. Include appropriate joins based on the join conditions specified in the input mapping table.
	5. Ensure correct data types and formatting (e.g., date conversions) for the selected columns.
	6. Store the generated SQL query in a suitable format for execution.

### FR-SQL-002: Generate SQL Query for Main Table "sales_orders"

* **Title:** Generate SQL Query for Main Table "sales_orders"
* **Description:** The system must generate a complete SQL SELECT statement that applies transformation logic, includes appropriate joins, and ensures correct data types and formatting for the main table "sales_orders".
* **Preconditions:**
	+ The intermediate table "s_shared.sales_orders_hdr_itm_lots" is available and populated with data.
	+ The source tables (e.g., "b_lots.plcam", "b_lots.plcbe") are available and contain the required data.
* **Main Flow / Functional Steps:**
	1. Retrieve the input mapping table with the required columns (Column Name, Column Type, Source Table, Source Column/Transformation Logic, Join Condition) for the main table.
	2. Parse the input mapping table to identify the required columns, data types, and transformation logic for the main table.
	3. Generate the SQL SELECT statement with the required columns, applying transformation logic as specified.
	4. Include appropriate joins based on the join conditions specified in the input mapping table, using the intermediate table "s_shared.sales_orders_hdr_itm_lots" as required.
	5. Ensure correct data types and formatting (e.g., date conversions) for the selected columns.
	6. Store the generated SQL query in a suitable format for execution.

The detailed functional requirements are as follows in bullet points:

* For FR-SQL-001:
	+ Retrieve input mapping table.
	+ Parse input mapping table.
	+ Generate SQL SELECT statement.
	+ Apply transformation logic.
	+ Include joins based on join conditions.
	+ Ensure correct data types and formatting.
* For FR-SQL-002:
	+ Retrieve input mapping table for main table.
	+ Parse input mapping table.
	+ Generate SQL SELECT statement.
	+ Apply transformation logic.
	+ Include joins based on join conditions using intermediate table.
	+ Ensure correct data types and formatting.