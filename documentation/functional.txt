Here is the detailed Functional Requirement Document (FRD) based on the provided Business Requirement Document (BRD) and adhering to the specified template:

### FR-SQLG-001: Generate SQL Query for Sales Orders Data

#### Title
Generate SQL Query for Sales Orders Data

#### Description
The system must generate a complete SQL SELECT statement that applies transformation logic, includes appropriate joins, and ensures correct data types and formatting based on the provided tabular mapping document.

#### Preconditions
- The input mapping table is available with the required columns (Column Name, Column Type, Source Table, Source Column/Transformation Logic, Join Condition).
- The source tables (e.g., `b_lots.plooh`, `b_lots.plool`, `b_lots.plcbe`, etc.) exist and contain the necessary data.
- The `s_shared.isc_registry` table contains the required registry codes and values.

#### Main Flow / Functional Steps

1. **Create Intermediate Table (`s_shared.sales_orders_hdr_itm_lots`):**
   - Retrieve the mapping document for `s_shared.sales_orders_hdr_itm_lots`.
   - Generate a SQL query that applies the specified transformation logic and join conditions.
   - Ensure the query includes all necessary columns as per the mapping document.

2. **Apply Transformation Logic and Joins for Intermediate Table:**
   - For each column in the mapping document, apply the specified transformation logic or directly map the source column.
   - Implement joins based on the join conditions provided (e.g., between `b_lots.plooh` and `b_lots.plool`).
   - Use aliases (e.g., `a`, `b`) consistently as indicated in the join conditions.

3. **Create Main Table (`sales_orders`):**
   - Retrieve the mapping document for `sales_orders`.
   - Generate a SQL query that joins `s_shared.sales_orders_hdr_itm_lots` with other required tables (e.g., `b_lots.plcam`, `b_lots.plcbe`, `s_shared.currency_conversion`) based on the specified join conditions.
   - Apply the transformation logic for each column as specified in the mapping document.

4. **Apply Filters and Additional Logic:**
   - Apply the filter `DATA_OPERATION <> 'D'` to all relevant tables.
   - For the `sales_orders` table, apply the additional filter `year(cast(to_date(from_unixtime(unix_timestamp(cast(a.datoxc as string), 'yyyyMMdd'))) as date)) >= year(current_date()) - 3`.

5. **Generate Final SQL Query:**
   - Combine the logic for creating the intermediate table and the main table into a coherent SQL query.
   - Ensure the query is formatted for readability.

6. **Output:**
   - The final output is a complete SQL query that can be executed to populate the `sales_orders` table.

#### Detailed SQL Generation Steps:

* For Step 1 (Creating Intermediate Table):
  - The system generates a SQL query like:
    ```sql
    CREATE TABLE s_shared.sales_orders_hdr_itm_lots AS
    SELECT 
      CONCAT(a.refto, a.refno) AS OrderNumber,
      a.stndx AS OrderType,
      b.bdcob AS SalesOrgCompanyCode,
      -- Apply similar logic for other columns
    FROM (SELECT * FROM b_lots.plooh WHERE DATA_OPERATION <> 'D') a
    LEFT JOIN b_lots.plool b ON TRIM(a.refto) = TRIM(b.refto) AND TRIM(a.refno) = TRIM(b.refno)
    -- Apply similar join logic for other tables as necessary
    WHERE a.DATA_OPERATION <> 'D';
    ```

* For Step 2 (Creating Main Table):
  - The system generates a SQL query like:
    ```sql
    CREATE TABLE sales_orders AS
    SELECT 
      a.OrderNumber,
      a.OrderType,
      -- Apply transformation logic for other columns based on the mapping document
      CASE 
        WHEN TRIM(e.whcda) <> '' AND e.whcda IS NOT NULL THEN e.whcda 
        ELSE b.whcdl 
      END AS SiteId,
      -- Other columns
    FROM s_shared.sales_orders_hdr_itm_lots a
    LEFT JOIN b_lots.plosl e ON -- Join conditions based on the mapping document
    -- Other joins and logic as specified
    WHERE a.DATA_OPERATION <> 'D' AND year(cast(to_date(from_unixtime(unix_timestamp(cast(a.datoxc as string), 'yyyyMMdd'))) as date)) >= year(current_date()) - 3;
    ```

The generated SQL queries will follow the logic and structure outlined in the mapping documents, ensuring that all transformation logic, join conditions, and filters are correctly applied.