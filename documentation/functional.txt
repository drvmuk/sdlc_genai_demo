Here is the detailed Functional Requirement Document (FRD) based on the provided Business Requirement Document (BRD) and the "Coding Requirements" section.

**FR-SQLG-001: Generate SQL Query for Sales Orders Data**

**Title:** Generate SQL Query for Sales Orders Data

**Description:** The system must generate a complete SQL query to extract sales orders data from various tables based on the provided mapping document and apply necessary transformations, joins, and filters.

**Preconditions:**

* The input mapping table is available with the required columns (Column Name, Column Type, Source Table, Source Column/Transformation logic, Join Condition, Common Filter).
* The source tables (e.g., `b_lots.plooh`, `b_lots.plool`, `s_shared.isc_registry`) exist and contain the required data.
* The necessary joins and filters are defined in the mapping document.

**Main Flow / Functional Steps:**

1. **Create Intermediate Table `s_shared.sales_orders_hdr_itm_lots`:**
	* Retrieve the mapping document for `s_shared.sales_orders_hdr_itm_lots`.
	* Apply the specified transformations, joins, and filters to generate the intermediate table.
	* Use aliases (e.g., `a`, `b`) consistently as per the join logic.
2. **Generate SQL Query for `s_shared.sales_orders_hdr_itm_lots`:**
	* Use the `SELECT ... FROM ... JOIN ...` syntax to generate the SQL query.
	* Include all necessary joins and filters (e.g., `DATA_OPERATION <> 'D'`).
	* Ensure correct data types and formatting (e.g., date conversions).
3. **Create Main Table `sales_orders`:**
	* Retrieve the mapping document for `sales_orders`.
	* Apply the specified transformations, joins, and filters to generate the main table.
	* Use the intermediate table `s_shared.sales_orders_hdr_itm_lots` as a source table.
4. **Generate SQL Query for `sales_orders`:**
	* Use the `SELECT ... FROM ... JOIN ...` syntax to generate the SQL query.
	* Include all necessary joins and filters.
	* Ensure correct data types and formatting.
5. **Combine the SQL Queries:**
	* Use the generated SQL queries for `s_shared.sales_orders_hdr_itm_lots` and `sales_orders` to create a single, complete SQL query.

**Detailed Requirements:**

* The system must apply the transformation logic as specified in the mapping document (e.g., `CONCAT(refto,refno)`).
* The system must include all necessary joins (e.g., `LEFT JOIN b_lots.plool b ON trim(a.refto)=trim(b.refto) AND trim(a.refno)=trim(b.refno)`).
* The system must apply the common filters (e.g., `DATA_OPERATION <> 'D'`) as specified in the mapping document.
* The system must ensure correct data types and formatting (e.g., date conversions using `cast(to_date(from_unixtime(unix_timestamp(cast(a.datoxc AS STRING), 'yyyyMMdd'))) AS DATE)`).
* The system must use aliases consistently as per the join logic.

**Expected Output:**

* A complete SQL query that extracts sales orders data from various tables based on the provided mapping document.
* The SQL query must be formatted for readability (indentation, line breaks).