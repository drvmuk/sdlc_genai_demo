Based on the provided Business Requirement Document (BRD), I will create a detailed Functional Requirement Document (FRD) in bullet points, adhering to the "Coding Requirements" section.

**FR-SOL-001: Create Intermediate Table s_shared.sales_orders_hdr_itm_lots**

* **Title:** Create Intermediate Table for Sales Orders
* **Description:** The system must create an intermediate table `s_shared.sales_orders_hdr_itm_lots` by joining and transforming data from various source tables.
* **Preconditions:**
	+ Source tables `b_lots.plooh`, `b_lots.plool`, and others are available and populated with data.
	+ Data in source tables meets the required format and quality standards.
* **Main Flow / Functional Steps:**
	1. Retrieve data from `b_lots.plooh` and apply filter `DATA_OPERATION <> 'D'`.
	2. Join `b_lots.plooh` with `b_lots.plool` on `refto` and `refno` columns.
	3. Apply transformations and calculations as specified in the mapping document.
	4. Create the intermediate table `s_shared.sales_orders_hdr_itm_lots` with the required columns.
	5. Populate the intermediate table with the transformed data.

**FR-SOL-002: Create Main Table sales_orders**

* **Title:** Create Main Table for Sales Orders
* **Description:** The system must create the main table `sales_orders` by transforming and joining data from the intermediate table `s_shared.sales_orders_hdr_itm_lots` and other source tables.
* **Preconditions:**
	+ Intermediate table `s_shared.sales_orders_hdr_itm_lots` is available and populated with data.
	+ Data in intermediate table meets the required format and quality standards.
* **Main Flow / Functional Steps:**
	1. Retrieve data from `s_shared.sales_orders_hdr_itm_lots` and apply filter `DATA_OPERATION <> 'D'` and `year(cast(to_date(from_unixtime(unix_timestamp(cast(a.datoxc as string), 'yyyyMMdd'))) as date))>=year(current_date())-3`.
	2. Join `s_shared.sales_orders_hdr_itm_lots` with other source tables as specified in the mapping document.
	3. Apply transformations and calculations as specified in the mapping document.
	4. Create the main table `sales_orders` with the required columns.
	5. Populate the main table with the transformed data.

Some specific functional steps for FR-SOL-002 include:

* Derive `SiteId` by joining `s_shared.sales_orders_hdr_itm_lots` with `b_lots.ploal` and `b_lots.plosl` on specific columns.
* Derive `SalesDistributionChannel` by joining `s_shared.sales_orders_hdr_itm_lots` with `b_lots.plcam` and `b_lots.plpms` on specific columns.
* Derive `ExchangeRate` by joining `s_shared.sales_orders_hdr_itm_lots` with `s_shared.currency_conversion` on specific columns.
* Derive `ActualShipDate` by joining `s_shared.sales_orders_hdr_itm_lots` with `b_lots.phihm` on specific columns.
* Derive `LatestProcessingDate` by joining `s_shared.sales_orders_hdr_itm_lots` with `b_lots.lecotw1` on specific columns.

These functional requirements outline the necessary steps to create the intermediate and main tables for sales orders, ensuring data is properly transformed and joined according to the specified mapping documents.