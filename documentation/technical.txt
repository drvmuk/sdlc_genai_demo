Here is the Technical Requirement Document (TRD) based on the provided Functional Requirement Document (FRD):

* **Technical Requirement ID**: TR-SQL-001
* **Related Functional Requirement(s)**: FR-SQL-001
* **Objective**: Implement a SQL query generator that creates a complete SQL query to extract sales orders data from various tables based on the provided mapping document.

* **Target Cluster Configuration**:
	+ Cluster Name: Sales Order Processing Cluster
	+ Databricks Runtime Version: 7.3.x-scala2.12
	+ Node Type: Standard_DS3_v2
	+ Driver Node: 1 x Standard_DS3_v2
	+ Worker Nodes: 2-5 x Standard_DS3_v2 (autoscaling)
	+ Autoscaling: Enabled
	+ Auto Termination: 30 minutes
	+ Libraries Installed: spark-sql, spark-core, scala-library

* **Source Data Details**:
	+ `b_lots.plooh` table is located in the database and is in a structured format.
	+ `b_lots.plool` table is located in the database and is in a structured format.
	+ `s_shared.isc_registry` table is located in the database and is in a structured format.
	+ Mapping document is available with the required columns (Column Name, Column Type, Source Table, Source Column/Transformation Logic, Join Condition, Common Filter).

* **Target Data Details**:
	+ `s_shared.sales_orders_hdr_itm_lots` intermediate table will be created in the database.
	+ `sales_orders` main table will be created in the database.
	+ Final SQL query will be generated as output.

* **Job Flow / Pipeline Stages**:
	+ Stage 1: Create intermediate table `s_shared.sales_orders_hdr_itm_lots` by applying transformation logic and join conditions specified in the mapping document.
	+ Stage 2: Create main table `sales_orders` by using the intermediate table and applying additional transformations and joins.
	+ Stage 3: Generate final SQL query by selecting required columns and applying necessary joins and filters.

* **Data Transformations / Business Logic**:
	+ Step 1: Retrieve data from `b_lots.plooh` and `b_lots.plool` tables and apply filters (e.g., `DATA_OPERATION <> 'D'`) and join conditions (e.g., `trim(a.refto)=trim(b.refto)`).
	+ Step 2: Perform transformations (e.g., `CONCAT(refto,refno)`, `cast(to_date(from_unixtime(unix_timestamp(cast(a.datoxc as string), 'yyyyMMdd'))) as date)`).
	+ Step 3: Retrieve data from `s_shared.sales_orders_hdr_itm_lots` and other tables (e.g., `b_lots.plcam`, `b_lots.plcbe`, `s_shared.currency_conversion`) and apply filters (e.g., `year(cast(to_date(from_unixtime(unix_timestamp(cast(a.datoxc as string), 'yyyyMMdd'))) as date))>=year(current_date())-3`).
	+ Step 4: Perform transformations (e.g., `IF(TRIM(PLCAM.SCTCX) <> '',PLCAM.SCTCX,PLPMS.SCPXXC)`, `cast(to_date(from_unixtime(unix_timestamp(cast(cur.from_date as string), 'yyyyMMdd'))) as date)`).

* **Error Handling and Logging**:
	+ Logging will be implemented to track any errors or exceptions during the execution of the SQL query generator.
	+ Error handling will be implemented to handle conditional logic (e.g., `IF`, `CASE`) and date conversions.